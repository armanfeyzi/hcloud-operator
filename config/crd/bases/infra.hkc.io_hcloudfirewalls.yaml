---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hcloudfirewalls.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudFirewall
    listKind: HCloudFirewallList
    plural: hcloudfirewalls
    shortNames:
    - hcfw
    singular: hcloudfirewall
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.firewallID
      name: FirewallID
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HCloudFirewall is the Schema for the hcloudfirewalls API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudFirewallSpec defines the desired state of an HCloudFirewall.
            properties:
              applyTo:
                description: ApplyTo configures server attachment. Omit to manage
                  rules only (attach servers elsewhere / manually).
                properties:
                  labelSelector:
                    description: |-
                      LabelSelector is a Hetzner Cloud server label selector string (e.g. "env=prod,app=my-app").
                      It matches labels on servers in Hetzner Cloud, not Kubernetes labels on CRs unless you sync them via HCloudServer.spec.labels.
                    type: string
                  serverRefs:
                    description: ServerRefs lists HCloudServer objects by name; the
                      controller resolves status.serverID and applies the firewall
                      to those servers.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels are attached to the Hetzner Cloud firewall resource.
                type: object
              rules:
                description: Rules is the ordered list of firewall rules (reconciled
                  with Set Rules on change).
                items:
                  description: HCloudFirewallRule defines one inbound or outbound
                    rule for a Hetzner Cloud Firewall.
                  properties:
                    description:
                      description: Description is an optional human-readable note
                        stored in Hetzner Cloud.
                      type: string
                    destinationIPs:
                      description: DestinationIPs lists IPv4/IPv6 CIDR strings for
                        outbound rules when applicable.
                      items:
                        type: string
                      type: array
                    direction:
                      description: Direction is "in" or "out" (Hetzner Cloud firewall
                        semantics).
                      enum:
                      - in
                      - out
                      type: string
                    port:
                      description: Port is a single port (e.g. "443"), a range ("8080-8090"),
                        or omitted for ICMP / some protocols.
                      type: string
                    protocol:
                      description: Protocol is tcp, udp, icmp, esp, or gre.
                      enum:
                      - tcp
                      - udp
                      - icmp
                      - esp
                      - gre
                      type: string
                    sourceIPs:
                      description: SourceIPs lists IPv4/IPv6 CIDR strings for matching
                        source traffic (e.g. "0.0.0.0/0").
                      items:
                        type: string
                      type: array
                  required:
                  - direction
                  - protocol
                  type: object
                type: array
            type: object
          status:
            description: HCloudFirewallStatus defines the observed state of an HCloudFirewall.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              firewallID:
                description: FirewallID is the Hetzner Cloud firewall ID.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
