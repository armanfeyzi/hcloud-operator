---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: hcloudcertificates.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudCertificate
    listKind: HCloudCertificateList
    plural: hcloudcertificates
    shortNames:
    - hccert
    singular: hcloudcertificate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.issuanceStatus
      name: Issuance
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HCloudCertificate is the Schema for the hcloudcertificates API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudCertificateSpec defines the desired state of an HCloudCertificate.
            properties:
              certificate:
                description: Certificate is the PEM-encoded certificate chain for
                  uploaded certificates.
                type: string
              domainNames:
                description: DomainNames lists domains for managed certificates.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to the Hetzner Cloud certificate resource.
                type: object
              privateKey:
                description: PrivateKey is the PEM-encoded private key for uploaded
                  certificates.
                type: string
              type:
                description: 'Type is the certificate type: uploaded or managed.'
                enum:
                - uploaded
                - managed
                type: string
            required:
            - type
            type: object
            x-kubernetes-validations:
            - message: type is immutable after creation
              rule: self.type == oldSelf.type
            - message: certificate and privateKey are required for uploaded certificates
              rule: self.type != 'uploaded' || (has(self.certificate) && has(self.privateKey))
            - message: domainNames are required for managed certificates
              rule: self.type != 'managed' || self.domainNames.size() > 0
          status:
            description: HCloudCertificateStatus defines the observed state of an
              HCloudCertificate.
            properties:
              certificateID:
                description: CertificateID is the Hetzner Cloud internal certificate
                  ID.
                format: int64
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the certificate.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              domainNames:
                description: DomainNames are the domains observed in Hetzner.
                items:
                  type: string
                type: array
              fingerprint:
                description: Fingerprint is the certificate fingerprint observed in
                  Hetzner.
                type: string
              issuanceStatus:
                description: IssuanceStatus reflects managed certificate issuance
                  (e.g. pending, completed, failed).
                type: string
              notValidAfter:
                description: NotValidAfter is when the certificate expires.
                format: date-time
                type: string
              notValidBefore:
                description: NotValidBefore is when the certificate becomes valid.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
