# Same pattern as k3s-day2-ccm / k3s-day2-csi: token used by cluster-autoscaler (Hetzner provider).
# Replace REPLACE_WITH_HCLOUD_TOKEN before apply.
apiVersion: v1
kind: Secret
metadata:
  name: hcloud
  namespace: kube-system
type: Opaque
stringData:
  token: REPLACE_WITH_HCLOUD_TOKEN
