{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "hcloud-operator.namespace" . }}
  labels:
    {{- include "hcloud-operator.labels" . | nindent 4 }}
{{- end }}
