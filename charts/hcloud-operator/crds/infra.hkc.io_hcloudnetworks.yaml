---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hcloudnetworks.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudNetwork
    listKind: HCloudNetworkList
    plural: hcloudnetworks
    shortNames:
    - hcn
    singular: hcloudnetwork
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ipRange
      name: IPRange
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HCloudNetwork is the Schema for the hcloudnetworks API.
          It represents a Hetzner Cloud private Network managed by the hcloud-operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudNetworkSpec defines the desired state of an HCloudNetwork.
            properties:
              exposeRoutesToVSwitch:
                description: ExposeRoutesToVSwitch exposes routes to an attached vSwitch
                  when applicable.
                type: boolean
              ipRange:
                description: IPRange is the IPv4 CIDR for the private network (e.g.
                  10.0.0.0/16).
                minLength: 9
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to the Hetzner Cloud network resource.
                type: object
              networkZones:
                description: |-
                  NetworkZones lists Hetzner network zones where a Cloud subnet is created (e.g. eu-central, us-east, us-west).
                  Subnets let you attach Cloud Servers in those zones to this network. Leave empty for a network with only the main IP range.
                items:
                  type: string
                maxItems: 16
                type: array
            required:
            - ipRange
            type: object
            x-kubernetes-validations:
            - message: ipRange is immutable after creation
              rule: self.ipRange == oldSelf.ipRange
            - message: networkZones is immutable after creation
              rule: '!has(oldSelf.networkZones) || self.networkZones == oldSelf.networkZones'
          status:
            description: HCloudNetworkStatus defines the observed state of an HCloudNetwork.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the network.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ipRange:
                description: IPRange is the CIDR observed in Hetzner (echo of allocation).
                type: string
              networkID:
                description: NetworkID is the Hetzner Cloud internal network ID.
                format: int64
                type: integer
              subnetZones:
                description: SubnetZones lists network zones where a Cloud subnet
                  now exists on this network.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
