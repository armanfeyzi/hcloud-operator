---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hcloudloadbalancers.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudLoadBalancer
    listKind: HCloudLoadBalancerList
    plural: hcloudloadbalancers
    shortNames:
    - hclb
    singular: hcloudloadbalancer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.loadBalancerType
      name: Type
      type: string
    - jsonPath: .status.publicIPv4
      name: IP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HCloudLoadBalancer is the Schema for the hcloudloadbalancers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudLoadBalancerSpec defines the desired state of an HCloudLoadBalancer.
            properties:
              algorithm:
                default: round_robin
                description: |-
                  Algorithm selects the balancing algorithm. Supported values are
                  "round_robin" and "least_connections".
                enum:
                - round_robin
                - least_connections
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to the Hetzner Cloud load balancer resource.
                type: object
              loadBalancerType:
                description: LoadBalancerType is the Hetzner load balancer type (e.g.
                  lb11, lb21, lb31).
                minLength: 1
                type: string
              location:
                description: Location is the Hetzner datacenter location (e.g. fsn1,
                  nbg1, hel1).
                type: string
              networkZone:
                description: NetworkZone is the Hetzner network zone (e.g. eu-central,
                  us-east).
                type: string
              serverSelector:
                description: |-
                  ServerSelector selects HCloudServer resources to attach as targets.
                  This matches the Kubernetes labels on HCloudServer objects.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              services:
                description: |-
                  Services defines front-end listeners, back-end ports, and optional health checks.
                  Reconciled by listen port: services not listed are removed from Hetzner.
                items:
                  description: HCloudLoadBalancerServiceSpec defines a load balancer
                    service (listener + target port).
                  properties:
                    certificateRefs:
                      description: CertificateRefs lists HCloudCertificate resources
                        attached to HTTPS listeners.
                      items:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            description: |-
                              Name of the referent.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      type: array
                    destinationPort:
                      description: DestinationPort is the port on target servers.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    healthCheck:
                      description: HealthCheck configures active health checking for
                        this service.
                      properties:
                        http:
                          description: HTTP configures HTTP(S) health check options.
                          properties:
                            domain:
                              description: Domain to send in the Host header.
                              type: string
                            path:
                              description: Path to request.
                              type: string
                            response:
                              description: Response substring to match in the response
                                body.
                              type: string
                            statusCodes:
                              description: StatusCodes lists acceptable HTTP status
                                codes (e.g. "2??", "3??").
                              items:
                                type: string
                              type: array
                            tls:
                              description: TLS enables TLS for the health check request.
                              type: boolean
                          type: object
                        intervalSeconds:
                          description: IntervalSeconds between health checks.
                          format: int32
                          minimum: 1
                          type: integer
                        port:
                          description: Port is the health check port. Defaults to
                            destinationPort when unset in Hetzner.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          description: Protocol is tcp, http, or https.
                          enum:
                          - tcp
                          - http
                          - https
                          type: string
                        retries:
                          description: Retries before marking a target unhealthy.
                          format: int32
                          minimum: 1
                          type: integer
                        timeoutSeconds:
                          description: TimeoutSeconds before a check is considered
                            failed.
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - protocol
                      type: object
                    listenPort:
                      description: ListenPort is the public port the load balancer
                        listens on.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    protocol:
                      description: Protocol is tcp, http, or https.
                      enum:
                      - tcp
                      - http
                      - https
                      type: string
                    proxyprotocol:
                      description: Proxyprotocol enables PROXY protocol for this service.
                      type: boolean
                  required:
                  - destinationPort
                  - listenPort
                  - protocol
                  type: object
                  x-kubernetes-validations:
                  - message: certificateRefs required when protocol is https
                    rule: self.protocol != 'https' || self.certificateRefs.size()
                      > 0
                type: array
                x-kubernetes-list-map-keys:
                - listenPort
                x-kubernetes-list-type: map
            required:
            - loadBalancerType
            type: object
            x-kubernetes-validations:
            - message: location and networkZone are mutually exclusive
              rule: '!has(self.location) || !has(self.networkZone)'
            - message: loadBalancerType is immutable after creation
              rule: self.loadBalancerType == oldSelf.loadBalancerType
            - message: location is immutable after creation
              rule: '!has(oldSelf.location) || !has(self.location) || self.location
                == oldSelf.location'
            - message: networkZone is immutable after creation
              rule: '!has(oldSelf.networkZone) || !has(self.networkZone) || self.networkZone
                == oldSelf.networkZone'
          status:
            description: HCloudLoadBalancerStatus defines the observed state of an
              HCloudLoadBalancer.
            properties:
              attachedServerIDs:
                description: AttachedServerIDs are server IDs currently attached as
                  targets.
                items:
                  format: int64
                  type: integer
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the load balancer's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              loadBalancerID:
                description: LoadBalancerID is the Hetzner Cloud internal load balancer
                  ID.
                format: int64
                type: integer
              publicIPv4:
                description: PublicIPv4 is the load balancer's public IPv4 address.
                type: string
              publicIPv6:
                description: PublicIPv6 is the load balancer's public IPv6 address.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
