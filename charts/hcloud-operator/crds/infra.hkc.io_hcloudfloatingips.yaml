---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hcloudfloatingips.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudFloatingIP
    listKind: HCloudFloatingIPList
    plural: hcloudfloatingips
    shortNames:
    - hcfip
    singular: hcloudfloatingip
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.ip
      name: IP
      type: string
    - jsonPath: .spec.serverRef.name
      name: Server
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HCloudFloatingIP is the Schema for the hcloudfloatingips API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudFloatingIPSpec defines the desired state of an HCloudFloatingIP.
            properties:
              description:
                description: Description is stored on the Hetzner Cloud floating IP
                  resource.
                type: string
              dnsPtr:
                description: DNSPtr sets reverse DNS for the floating IP address.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to the Hetzner Cloud floating IP resource.
                type: object
              location:
                description: Location is the Hetzner location for this floating IP
                  (e.g. fsn1).
                minLength: 1
                type: string
              serverRef:
                description: ServerRef points to an HCloudServer to assign this floating
                  IP to.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              type:
                description: 'Type is the floating IP address family: ipv4 or ipv6.'
                enum:
                - ipv4
                - ipv6
                type: string
            required:
            - location
            - type
            type: object
            x-kubernetes-validations:
            - message: type is immutable after creation
              rule: self.type == oldSelf.type
            - message: location is immutable after creation
              rule: self.location == oldSelf.location
          status:
            description: HCloudFloatingIPStatus defines the observed state of an HCloudFloatingIP.
            properties:
              appliedServerID:
                description: AppliedServerID is the Hetzner server ID this floating
                  IP is assigned to.
                format: int64
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the floating IP.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              floatingIPID:
                description: FloatingIPID is the Hetzner Cloud internal floating IP
                  ID.
                format: int64
                type: integer
              ip:
                description: IP is the allocated address observed in Hetzner.
                type: string
              location:
                description: Location is the home location observed in Hetzner.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
