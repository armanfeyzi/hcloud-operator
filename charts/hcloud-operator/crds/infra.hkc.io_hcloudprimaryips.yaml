---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hcloudprimaryips.infra.hkc.io
spec:
  group: infra.hkc.io
  names:
    kind: HCloudPrimaryIP
    listKind: HCloudPrimaryIPList
    plural: hcloudprimaryips
    shortNames:
    - hcpip
    singular: hcloudprimaryip
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.ip
      name: IP
      type: string
    - jsonPath: .spec.serverRef.name
      name: Server
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HCloudPrimaryIP is the Schema for the hcloudprimaryips API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCloudPrimaryIPSpec defines the desired state of an HCloudPrimaryIP.
            properties:
              autoDelete:
                description: AutoDelete deletes the primary IP when the assignee is
                  deleted.
                type: boolean
              datacenter:
                description: Datacenter is the Hetzner datacenter for this primary
                  IP (e.g. fsn1-dc14).
                minLength: 1
                type: string
              dnsPtr:
                description: DNSPtr sets reverse DNS for the primary IP address.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels to attach to the Hetzner Cloud primary IP resource.
                type: object
              serverRef:
                description: ServerRef points to an HCloudServer to assign this primary
                  IP to.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              type:
                description: 'Type is the primary IP address family: ipv4 or ipv6.'
                enum:
                - ipv4
                - ipv6
                type: string
            required:
            - datacenter
            - type
            type: object
            x-kubernetes-validations:
            - message: type is immutable after creation
              rule: self.type == oldSelf.type
            - message: datacenter is immutable after creation
              rule: self.datacenter == oldSelf.datacenter
          status:
            description: HCloudPrimaryIPStatus defines the observed state of an HCloudPrimaryIP.
            properties:
              appliedAssigneeID:
                description: AppliedAssigneeID is the Hetzner assignee ID currently
                  managed via spec.serverRef.
                format: int64
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the primary IP.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              datacenter:
                description: Datacenter is the datacenter observed in Hetzner.
                type: string
              ip:
                description: IP is the allocated address observed in Hetzner.
                type: string
              primaryIPID:
                description: PrimaryIPID is the Hetzner Cloud internal primary IP
                  ID.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
